---
name: Keep deployment alive

on:
  schedule:
    - cron: "14 3 * * *"

jobs:
  ping:
    runs-on: ubuntu-slim
    steps:
      - run: curl --fail 'https://starter-kit-ci.herokuapp.com/api/message'
